/**
 * Раздел «Руны» временно скрыт.
 * Прежняя заглушка с текстом про Swagger — в истории git; для UI см. закомментированный блок в `src/api/mysticApi.ts` (рунные расклады).
 */
export function RunesPage() {
  return null;
}
